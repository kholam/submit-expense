
// props for the modal
interface ModalProps {
    handleCloseModal: () => void; // contains the state for managing and close of the modal
    isFormValid?: boolean; // validity of the form
}

export default ModalProps;